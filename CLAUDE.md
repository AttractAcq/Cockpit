# CLAUDE.md — AA Cockpit (Reel Studio era)

This file gives Claude Code persistent context for the **Cockpit repo**. Read it at the start of every session.

**Everything below section "Superseded" in this file describing a 7-workspace / `entities` / `campaigns` / MRR architecture is retired.** That architecture does not exist in the live database and its referenced spec docs (`docs/attract-acquisition-backend.md`, `-frontend_1.md`, `-system-map_1.md`) no longer exist in this repo. Do not rebuild it.

## Repo / remote / project identity — verify every session

- **This repo is the working repo for Cockpit app code.** Repo root: `/Users/alex/Desktop/Attract Acq/Application Surfaces/Cockpit`
- **Remote:** `https://github.com/AttractAcq/Cockpit` (origin)
- **Production Supabase project:** `xivewedajschthjlblfb`
- Broader workspace/business instructions live in the root `CLAUDE.md` at `/Users/alex/Desktop/Attract Acq/CLAUDE.md` — that file wins on positioning, offers, commercial authority, and workspace-wide build-track status. This file wins on Cockpit repo/app specifics.

**Before starting any build session, verify:**
1. `pwd` and `git rev-parse --show-toplevel` match the repo root above.
2. `git remote -v` includes `AttractAcq/Cockpit`.
3. `git status --short` is clean (or every non-clean entry is explained/expected).
4. The target Supabase project is `xivewedajschthjlblfb`.
5. Stop and report if any of the above don't match — do not proceed from the wrong repo/project.

## Golden architecture rules (load-bearing — never violate)

- **Surfaces are thin.** This app renders data and captures intent. It does **not** call external APIs (Meta, Higgsfield, Anthropic, OpenAI, etc.) directly and **never holds secrets**.
- **Every action that touches the outside world or mutates state invokes an edge function** via `supabase.functions.invoke(...)`.
- **Reads come from Supabase directly**; **writes go through edge functions.**
- **Multi-tenant isolation is enforced by RLS at the database**, not in app code.
- Read Supabase URL + anon key from env (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`). Do not hardcode credentials.
- No secrets printed to logs or committed to the repo. No credentials stored in client-facing settings tables.

## Build & commands

```bash
npm install
npm run dev        # local dev server
npm run typecheck  # tsc --noEmit — run after every change
npm run build      # production build
```

Conventions: named function exports, one component per file, PascalCase filenames; absolute imports via `@/`; never `any` (prefer `unknown` + narrow); Tailwind utility classes only; never replace a real Supabase query with a mock to make something pass.

## Current build status

**All previous Cockpit build tracks (Client Context OS Phase 1/2, Batch A–D1) are closed** unless explicitly reopened by Alex.

**Active authorized build: Reel Studio (AI video generation).**

- **Phase A (schema): complete, live.** Migration `20260722203942_reel_studio_phase_a_foundations.sql` — tables `brand_prompt_blocks`, `video_projects`, `video_shots`, `generation_credits_ledger`; storage bucket `video-assets` (private). RLS: staff (`admin`/`account_manager`/`editor`) SELECT-only; writes intended via edge functions (service role). No anon access.
  - **Corrected 2026-07-23** by two additive migrations (never edit `20260722203942` in place): `20260723111327_reel_studio_phase_a_two_step_pipeline.sql` and `20260723111600_reel_studio_still_image_bucket.sql`. Reason: Higgsfield's DoP model family (the only video model confirmed available on the account, labeled "Image to Video" on Higgsfield's own dashboard) is **image-to-video, not text-to-video** — it requires a source still image plus a camera-motion directive. The real per-shot pipeline is two Higgsfield calls, not one: (1) text-to-image (Soul Standard / Popcorn Auto) → still frame, (2) DoP image-to-video using that still + a motion directive → final clip.
  - `video_shots` gained: `still_image_url`, `still_image_job_id`, `still_image_model`, `motion_type`, `motion_strength` (0–1). `status` CHECK expanded to `pending → still_submitted → still_rendering → still_complete → submitted → rendering → complete/failed`. The `video_shots_active_idx` partial index now also covers `still_submitted`/`still_rendering`. The `video-assets` bucket's `allowed_mime_types` now also includes `image/jpeg`/`image/png`/`image/webp` (same bucket holds both stills and clips; RLS unchanged, scoped by `bucket_id` not mime type).
  - No Higgsfield credentials or video jobs exist yet.
- **Phase B (two-step Higgsfield generation loop): deployed and live-tested end-to-end, 2026-07-23.** All 4 functions deployed to `xivewedajschthjlblfb`; one real still-image job + one real draft-tier DoP video job were run against the real Higgsfield API using disposable test fixtures (since deleted, both from `video-assets` storage and the DB, along with the temporary diagnostic function and test auth user used to run this). Both stages reached `complete` with real files downloaded into `video-assets` before cleanup. Confirmed: `higgsfield-ai/soul/standard` (still-image) and `higgsfield-ai/dop/lite` (DoP draft tier) are both valid, working model_ids. `higgsfield-ai/dop/standard` (final tier) remains unconfirmed. Auth is `Authorization: Key {api_key}:{api_key_secret}`; submit is `POST https://platform.higgsfield.ai/{model_id}`; status is `GET https://platform.higgsfield.ai/requests/{request_id}/status`; async queue lifecycle `queued → in_progress → completed/failed/nsfw`; webhooks via `?hf_webhook={url}` query param, retried 2h, **no signature verification documented** (functions re-fetch status themselves rather than trust the webhook body — poll-only, no webhook wired yet, deliberately). **Two real corrections found via live 422s, since fixed in `_shared/higgsfield.ts` and `submit-shot-generation`:** the DoP request body field is `image_url` not `image`, and the motion field is `motions[].id` not `motions[].motion` — critically, that `id` must be an actual UUID from Higgsfield's motion catalog, not a free-text slug. **New endpoint, found empirically (undocumented anywhere):** `GET https://platform.higgsfield.ai/v1/motions` lists the full motion catalog (`{id, name, description, preview_url, start_end_frame}`, 121 entries). No lookup/caching layer exists yet for this in the repo — `video_shots.motion_type` currently expects a raw catalog UUID to be written directly (open design question for Phase C: keep as opaque UUID, or add an internal name→UUID mapping, possibly connected to the existing-but-unwired `brand_prompt_blocks.motion_block` column). Other real gaps still open: no documented per-request credit-cost field (so `generation_credits_ledger` is still not written), generated files retained only 7 days minimum (confirms we must download into `video-assets` promptly, which the deployed code already does). See memory file `higgsfield-api.md` for full detail.
  - Draft code, two function pairs (submit + check per stage, same claim/submit/poll convention as `generate-ai-background-image`/`check-ai-background-image` — conditional `UPDATE ... WHERE status = X` as the atomic claim):
    - Stage 1 (still image): `supabase/functions/submit-shot-still-image/index.ts` (claims one `pending` shot, calls `submitHiggsfieldTextToImage` with `HIGGSFIELD_MODEL_STILL`, stores `still_image_job_id`/`still_image_model`), `supabase/functions/check-shot-still-image/index.ts` (polls one `still_submitted`/`still_rendering` shot, downloads the still into `video-assets` as `{clientId}/{video_project_id}/{shotId}-still.<ext>`, sets `still_image_url`, marks `still_complete`/`failed`).
    - Stage 2 (DoP video): `supabase/functions/submit-shot-generation/index.ts` (claims one `still_complete` shot, requires `still_image_url` + `motion_type` + `motion_strength` to already be set — fails closed otherwise, mints a 1h signed URL for the still since the bucket is private, calls `submitHiggsfieldImageToVideo` with `image` + `motions: [{motion, strength}]`, stores `higgsfield_job_id`), `supabase/functions/check-shot-generation/index.ts` (polls one `submitted`/`rendering` shot, downloads the completed clip into `video-assets`, marks `complete`/`failed` — unchanged from the original draft).
    - Shared adapter: `supabase/functions/_shared/higgsfield.ts` (plain-`fetch` REST adapter, no SDK) — `submitHiggsfieldTextToImage` (stage 1, prompt-only), `submitHiggsfieldImageToVideo` (stage 2, requires `image`/`motions`), `checkHiggsfieldGeneration` (shared by both stages — still-image status returns `images`, DoP status returns `video`).
  - Config uses plain Supabase secrets (`Deno.env.get(...)`), matching the existing `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` convention — **not** the per-tenant `vault_read_credential`/`_GLOBAL_` Vault convention (that convention is for client-scoped service credentials; global AI-provider keys in this codebase are already plain env secrets). Required secrets, none set yet: `HIGGSFIELD_API_KEY` + `HIGGSFIELD_API_SECRET` (read as two separate secrets and assembled into the documented `Key {api_key}:{api_key_secret}` header — never one pre-joined value), `HIGGSFIELD_MODEL_STILL` (text-to-image model_id, single, tier-independent), `HIGGSFIELD_MODEL_DRAFT`, `HIGGSFIELD_MODEL_FINAL` (DoP model_id per render tier — fails closed with a 503 if unset rather than guessing a model_id).
  - Not yet done: no `motion_type`/`motion_strength` UI to populate those columns (Phase C); no webhook wiring (poll-only, deliberately, given the unverified-webhook gap); no `generation_credits_ledger` writes (Higgsfield's status response has no documented per-request credit-cost field); none of the four functions have been deployed or called against a real Higgsfield account.
- **Phase C (orchestration + Studio UI): deployed and live-tested end-to-end, 2026-07-23.** All 6 new edge functions (`create-video-project`, `create-video-shot`, `update-video-shot`, `delete-video-shot`, `update-video-project-status`, `list-higgsfield-motions`) deployed and ACTIVE on `xivewedajschthjlblfb`; reused Phase B functions untouched. New `reel_studio` tab added to `ClientDetailPage.tsx` (`ReelStudioPanel.tsx`), plus a "Produce as Reel Studio Video" action on `MastersPanel.tsx`. Security advisors run post-deploy: zero new issues attributable to Phase C. Full UI smoke test run against real data (client "Attract Acquisition", source `organic_master` row `JUL26-CR-001`, real Higgsfield API calls, disposable `ZZ`-prefixed fixture project/shot + throwaway admin auth user, all deleted afterward): project creation → shot creation with the live motion-catalog picker → "Generate still"/"Check still" (`still_submitted → still_rendering → still_complete`) → "Generate video"/"Check video" (`submitted → rendering → complete`, real `.mp4` downloaded into `video-assets`) → project status transition (`storyboarding → generating`) all confirmed working through the UI, matching the plan's design exactly. Known caveat carried forward, not fixed: the only active `brand_prompt_blocks` row still hardcodes South African content, conflicting with current EUR/Europe commercial authority — flagged to Alex, not a Phase C bug. Two orphaned test files remain in the private `video-assets` bucket (`storage.objects` has delete protection — direct SQL delete is blocked; needs Storage-API cleanup, not blocking, low priority).
- **Phase D (handoff + telemetry): complete and live-tested end-to-end, 2026-07-23.** New edge function `handoff-video-project` deployed to `xivewedajschthjlblfb` — the sole legitimate path that turns a completed (`approved`) Reel Studio project into AI-produced `client_assets` on a real, already-approved `reel_video` `client_production_briefs` row. It never auto-creates a brief; it fails closed (409) unless every shot is `complete` with a `clip_url`, a matching approved `reel_video` brief exists (or is already linked), and there's exactly one such brief for the source row. On success it inserts one `client_assets` row per shot (`status: needs_review`, `storage_bucket: video-assets`, `width/height: 1080x1920` matching the 9:16 contract, `mime_type: video/mp4`), sets the brief's `production_mode: ai` / `production_status: produced`, archives the previous asset group, marks the project `handed_off`, and logs `activity_log` (`event_type: reel_studio_handoff`) — with rollback-on-catch cleanup of any partial `client_assets` inserts. `update-video-project-status` was tightened so `approved → handed_off` is no longer reachable through its generic whitelist (`ALLOWED_TRANSITIONS.approved` is now empty) — `handed_off` is only ever set by `handoff-video-project`. Frontend: `ReelStudioPanel.tsx` mirrors the tightened whitelist and adds a "Hand off to production" button (shown only when `status === "approved"`); `src/lib/api.ts` gained `handoffVideoProject()`.
  - **Three real DB-level constraint gaps found and fixed via live testing** (not caught by `npm run typecheck`/`npm run build`, since they're runtime Postgres CHECK violations, not TS type errors) — all three constraints predated Phase D and only ever anticipated the old synchronous AI-**image** pipeline, never a video clip: `client_assets_asset_format_check` didn't allow `'reel_video'` (unlike the equivalent constraint on `client_production_briefs`, which already did); `client_assets_mime_type_check` only allowed `image/png|jpeg|webp`; `client_assets_storage_bucket_check` was hardcoded to `'client-assets'` only. Fixed via three additive migrations (each drops and re-adds the constraint with the missing value included): `reel_studio_phase_d_client_assets_reel_video_format`, `reel_studio_phase_d_client_assets_video_mime_type`, `reel_studio_phase_d_client_assets_video_bucket`.
  - Live smoke test used disposable `ZZ`-prefixed fixtures (organic_master row, client_production_briefs row, video_projects/video_shots rows, a throwaway admin auth user) tied to the real client `Attract Acquisition` — real handoff call produced a real `client_assets` row with correct fields, confirmed the direct `approved → handed_off` transition now correctly 409s, then all fixtures (including the auth user) were deleted. Security advisors re-run post-migrations: zero new issues (all warnings pre-existing, unrelated to Phase D).
  - `generation_credits_ledger` writes remain deliberately deferred (unchanged from Phase B — Higgsfield still exposes no per-request credit-cost field).
  - **Reel Studio (Phases A–D) is now complete.** No new phase is currently authorized — do not start further Reel Studio work without explicit sign-off.
- **Post-Phase-D flow change (2026-07-23): standalone projects + Content Briefs → Reel Studio redirect, deployed and live-tested.**
  - `create-video-project`'s `source_table`/`source_row_id` are now optional (both omitted = a standalone, sourceless project; both present = tied to an `organic_master`/`ads_master` row as before; exactly one present = 400). Migration `reel_studio_standalone_project_source_optional` relaxed `video_projects_source_pair` from a strict XOR to "at most one" (`organic_master_id`/`ads_master_id` may now both be null). Deployed as `create-video-project` v2.
  - `ReelStudioPanel.tsx`'s "New project" button (no prefill) now renders no source-row fields at all. The Content Briefs tab's "Produce asset" popup no longer blocks the "AI" option for `reel_video` briefs — clicking it now navigates into Reel Studio's New Project modal pre-filled with the brief's `organic_master` source row and title (via `reel_source_table`/`reel_source_row_id`/`reel_title`/`reel_production_brief_id` URL params), instead of generating an AI image. The Human produce flow for `reel_video` is untouched.
  - When a project is created from that redirect, `ReelStudioPanel.tsx`'s `onCreated` calls the pre-existing `transitionContentCreationToAssets` (unchanged, reused as-is) to write a `client_asset_pipeline_state` row with `current_stage: "assets"` for the brief's `source_ref` — this makes the originating brief show as "Passed Through" on the Content Briefs tab immediately, before any `client_assets` row exists (real handoff/production still happens later, unchanged, via `handoff-video-project`).
  - Live-tested end-to-end via direct edge-function/REST calls with disposable `ZZ`-prefixed fixtures + a temp admin auth user (all deleted after): standalone project created with both `organic_master_id`/`ads_master_id` null; brief-redirect project created with `organic_master_id` set, and the pipeline-state write correctly flipped `isPassedThrough` to true. `get_advisors` (security) showed no new issues post-deploy.
  - **Gotcha reconfirmed**: `deploy_edge_function`'s `files` array must name shared imports using the exact literal relative path the entrypoint uses (e.g. `../_shared/aa.ts`, not `_shared/aa.ts`) — the entrypoint is bundled under a `source/` subdirectory, so omitting the `../` prefix fails with a "Module not found" bundling error even though the file is present in the array.

### The `reel_video` / `humanOnly` gate

The live codebase still hard-rejects `reel_video`-format production briefs as human-only through the old synchronous AI-**image** pipeline (`_shared/ai-asset-generation.ts`, `_shared/production-brief-contract.ts`, and the other touchpoints from the original reconciliation). **This gate is intentionally still in place and was NOT relaxed** — the 2026-07-23 flow change above added one new, narrow path alongside it (Content Briefs → Reel Studio redirect → `create-video-project` → eventual `handoff-video-project`), it did not touch or bypass this gate's own code paths:
- **Do not casually remove or relax this gate.** It is a known, deliberate product rule today for the old image pipeline.
- No video generation, publishing, scheduling, Phase 3 changes, or worker/cron changes should happen without an explicit gate/sign-off.

## Reference docs (load on demand)

- `docs/EDGE_FUNCTIONS_REFERENCE.md`, `docs/PHASE_3_COCKPIT_WIRING_PLAN.md`, `docs/PREFLIGHT_READINESS.md`, `docs/VAULT_SECRET_RECONCILIATION.md` — current-era references; verify against live state before trusting fully.
- `docs/reconciliation-report.md` is **deprecated** (describes the retired 7-workspace/`entities`/`campaigns` architecture as of 2026-06-21; superseded by the Client Context OS schema and now by Reel Studio). See the deprecation notice at the top of that file.
